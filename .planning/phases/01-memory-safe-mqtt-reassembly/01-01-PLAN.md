---
phase: 01-memory-safe-mqtt-reassembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/connectivity/mqtt_dataplane.c
  - main/connectivity/mqtt_dataplane.h
  - main/Kconfig.projbuild
  - sdkconfig.defaults
  - docs/manual-test-plan.md
autonomous: true
must_haves:
  truths:
    - "Device processes fragmented MQTT payloads without per-fragment heap allocation in the ingest/reassembly hot path."
    - "Device deterministically drops oversize, out-of-order, or unsupported fragments and emits explicit counters with reasoned logs."
    - "Device allows only one active fragmented reassembly flow so overlapping flows cannot increase RAM pressure."
  artifacts:
    - path: "main/connectivity/mqtt_dataplane.c"
      provides: "Single-slot fixed-capacity reassembly state machine and drop taxonomy implementation"
      contains: "MQTT_EVENT_DATA"
    - path: "main/connectivity/mqtt_dataplane.h"
      provides: "Public dataplane drop-counter snapshot type/API"
      contains: "mqtt_dataplane"
    - path: "main/Kconfig.projbuild"
      provides: "Bounded fragmented reassembly size configuration"
      contains: "CONFIG_THEO_MQTT_REASSEMBLY_MAX_BYTES"
    - path: "sdkconfig.defaults"
      provides: "Default fixed bound for reassembly buffer"
      contains: "CONFIG_THEO_MQTT_REASSEMBLY_MAX_BYTES"
  key_links:
    - from: "main/connectivity/mqtt_dataplane_event_handler"
      to: "main/connectivity/mqtt_dataplane_task"
      via: "xQueueSend of fixed-size fragment metadata/payload"
      pattern: "xQueueSend\\(s_msg_queue"
    - from: "main/connectivity/handle_fragment_message"
      to: "main/connectivity/process_payload"
      via: "completed validated single-slot reassembly"
      pattern: "process_payload"
    - from: "main/connectivity/validation path"
      to: "drop counters/logs"
      via: "ordered reject reasons with reset"
      pattern: "ESP_LOGW\\(TAG, \"drop reason="
---

<objective>
Implement bounded, memory-safe MQTT fragmented-payload reassembly so the dataplane stays stable under malformed or overlapping traffic.

Purpose: Satisfy MEM-01, MEM-02, and MEM-03 by replacing heap-churn and multi-flow behavior with deterministic fixed-capacity processing.
Output: Single-slot reassembly FSM, explicit drop counters/logging, and config/docs updates for the new memory bounds.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-memory-safe-mqtt-reassembly/01-RESEARCH.md
@main/connectivity/mqtt_dataplane.c
@main/connectivity/mqtt_dataplane.h
@main/Kconfig.projbuild
@sdkconfig.defaults
@docs/manual-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace heap-based fragment intake with fixed-capacity single-slot reassembly</name>
  <files>main/connectivity/mqtt_dataplane.c</files>
  <action>Refactor MQTT fragment ingest and reassembly to remove per-fragment/per-message malloc/calloc/free from the hot path. Use one pre-allocated reassembly context (IDLE/ASSEMBLING style behavior) with fixed topic + payload storage and strict offset progression. Keep exactly one active fragmented flow at a time (drop overlapping concurrent flows) per MEM-03 and the research lock to prioritize memory stability over payload salvage. Validate total length, offset, and fragment length before every copy; reject invalid fragments before state mutation.</action>
  <verify>Run `idf.py build` and confirm no build errors; inspect `main/connectivity/mqtt_dataplane.c` to ensure MQTT_EVENT_DATA handling no longer calls malloc/calloc/free for fragment data or reassembly buffers.</verify>
  <done>Fragmented payload handling uses bounded pre-allocated storage only, with single active flow enforcement and validate-before-copy behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic drop taxonomy with counters and structured logs</name>
  <files>main/connectivity/mqtt_dataplane.c, main/connectivity/mqtt_dataplane.h</files>
  <action>Add explicit drop reasons and counters for at least oversize, out_of_order, unsupported_overlap/concurrent_flow, missing_topic_on_first, unsupported_initial_nonzero_offset, and queue_full. Increment exactly one reason per dropped fragment and emit structured ESP_LOGW lines including reason, msg_id, offset, fragment_len, and total_len. Add a lightweight dataplane stats snapshot API in the header so counters are queryable without Phase 2 observability scope expansion.</action>
  <verify>Run `idf.py build`; verify logs/counter updates are wired in each reject branch by checking all invalid-fragment exits increment a reasoned counter before reset/return.</verify>
  <done>All MEM-02 rejection classes are deterministic, counted, and logged with reason-specific evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Wire bounded configuration defaults and add targeted manual verification steps</name>
  <files>main/Kconfig.projbuild, sdkconfig.defaults, docs/manual-test-plan.md</files>
  <action>Add `CONFIG_THEO_MQTT_REASSEMBLY_MAX_BYTES` (and any minimal companion bound needed for topic storage) to Kconfig with safe ranges/defaults, then mirror defaults in `sdkconfig.defaults`. Extend `docs/manual-test-plan.md` with a focused fragmented-MQTT validation section covering: in-order valid fragmentation, oversize drop, out-of-order drop, non-zero-offset-first drop, and overlapping-flow drop; include expected counter/log outcomes for each case. Keep scope tight to MEM-01/02/03 only and defer Phase 2+ observability ideas.</action>
  <verify>Run `idf.py build` after config additions; review `docs/manual-test-plan.md` to ensure each MEM drop class has explicit steps and expected evidence.</verify>
  <done>Configurable memory bounds are available in Kconfig/defaults and manual verification steps fully cover the Phase 1 fragmented-flow success/failure matrix.</done>
</task>

</tasks>

<verification>
1) `idf.py build` passes with new dataplane and Kconfig changes.
2) Code inspection confirms no per-fragment dynamic allocation remains in fragmented receive/reassembly path.
3) Manual test plan includes deterministic validation for all required drop classes and single-flow enforcement.
</verification>

<success_criteria>
1) MEM-01: Fragment reassembly path executes with fixed pre-allocated capacity and no hot-path heap churn.
2) MEM-02: Oversize/out-of-order/unsupported fragments are deterministically dropped with explicit reason counters + logs.
3) MEM-03: Only one fragmented flow can be active; overlapping flows are rejected without additional RAM pressure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-safe-mqtt-reassembly/01-memory-safe-mqtt-reassembly-01-SUMMARY.md`
</output>
