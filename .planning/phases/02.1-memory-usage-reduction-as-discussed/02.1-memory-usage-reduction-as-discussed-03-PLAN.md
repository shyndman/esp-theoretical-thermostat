---
phase: 02.1-memory-usage-reduction-as-discussed
plan: 03
type: execute
wave: 3
depends_on:
  - 02.1-memory-usage-reduction-as-discussed-02
files_modified:
  - main/Kconfig.projbuild
  - sdkconfig.defaults
  - main/connectivity/mqtt_dataplane.c
  - main/connectivity/runtime_health.h
  - main/connectivity/runtime_health.c
  - main/sensors/env_sensors.c
  - main/streaming/webrtc_stream.c
  - main/streaming/whep_endpoint.c
  - main/connectivity/device_telemetry.c
  - docs/manual-test-plan.md
autonomous: true

must_haves:
  truths:
    - "MQTT queue-depth reduction is available behind a default-off wave-2 gate and reduces internal-RAM footprint when enabled."
    - "Wave-3 includes explicit fragmentation-risk reduction in hot paths, gated default-off, with largest-free-block trend evidence."
    - "Stack right-sizing follows measured high-water plus margin policy and is gated default-off for wave-3 trials."
    - "Per-bundle and combined-bundle validation instructions exist before attempting default flips."
  artifacts:
    - path: "main/connectivity/mqtt_dataplane.c"
      provides: "Config-gated MQTT queue-depth profile switch for deterministic RAM savings"
      contains: "MQTT_DP_QUEUE_DEPTH"
    - path: "main/streaming/whep_endpoint.c"
      provides: "Hot-path allocation reuse strategy to reduce heap churn and fragmentation risk"
      contains: "malloc|free"
    - path: "main/connectivity/runtime_health.c"
      provides: "Measured high-water evidence and largest-free-block fragmentation trend inputs for wave-3 tuning"
      contains: "largest_free_block"
    - path: "main/Kconfig.projbuild"
      provides: "Wave-2 and wave-3 default-off tuning gates"
      contains: "CONFIG_THEO_RAM_WAVE2_MQTT_QUEUE_TUNING"
  key_links:
    - from: "main/Kconfig.projbuild"
      to: "main/connectivity/mqtt_dataplane.c"
      via: "Wave-2 gate selects reduced queue depth profile"
      pattern: "CONFIG_THEO_RAM_WAVE2_MQTT_QUEUE_TUNING"
    - from: "main/connectivity/runtime_health.c"
      to: "main/sensors/env_sensors.c"
      via: "Observed stack headroom drives measured stack+margin sizing updates"
      pattern: "uxTaskGetStackHighWaterMark2"
    - from: "main/connectivity/runtime_health.c"
      to: "main/streaming/webrtc_stream.c"
      via: "Observed stack headroom drives measured stack+margin sizing updates"
      pattern: "webrtc"
    - from: "main/connectivity/runtime_health.c"
      to: "main/streaming/whep_endpoint.c"
      via: "Largest-free-block/fragmentation trend validates hot-path reuse changes"
      pattern: "largest|fragment"
---

<objective>
Implement wave-2 MQTT queue reduction and wave-3 measured stack/secondary reductions as default-off tuning bundles.

Purpose: Deliver the second locked reduction priority (MQTT queue depth) and the next largest measured RAM win without removing required capabilities.
Output: Two additional default-off gates, queue-depth reduction profile, explicit fragmentation-reduction changes, measured stack+margin reductions, and updated bundle validation matrix.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-CONTEXT.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-RESEARCH.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-02-SUMMARY.md
@main/connectivity/mqtt_dataplane.c
@main/connectivity/runtime_health.c
@main/sensors/env_sensors.c
@main/streaming/webrtc_stream.c
@main/connectivity/device_telemetry.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add default-off wave-2 gate for MQTT queue-depth reduction</name>
  <files>main/Kconfig.projbuild, sdkconfig.defaults, main/connectivity/mqtt_dataplane.c</files>
  <action>Add `CONFIG_THEO_RAM_WAVE2_MQTT_QUEUE_TUNING` with `default n`, and implement a gated queue-depth profile in `mqtt_dataplane.c` (baseline unchanged when off, reduced depth when on). Keep existing fragmentation/reassembly contract intact and preserve queue-full/drop diagnostics so reduced-depth behavior remains visible during validation.</action>
  <verify>`idf.py build` succeeds with wave-2 flag disabled and enabled; dataplane logs still include drop diagnostics under burst load.</verify>
  <done>Wave-2 queue-depth reduction is isolated behind a default-off gate with deterministic RAM savings and intact observability.</done>
</task>

<task type="auto">
  <name>Task 2: Apply wave-3 default-off stack right-sizing plus explicit fragmentation reduction</name>
  <files>main/Kconfig.projbuild, sdkconfig.defaults, main/connectivity/runtime_health.h, main/connectivity/runtime_health.c, main/connectivity/mqtt_dataplane.c, main/sensors/env_sensors.c, main/streaming/webrtc_stream.c, main/streaming/whep_endpoint.c, main/connectivity/device_telemetry.c</files>
  <action>Add `CONFIG_THEO_RAM_WAVE3_STACK_RIGHTSIZE` with `default n`, then implement stack sizing branches for high-impact tasks (mqtt dataplane, env sensors, webrtc worker, device telemetry) using observed high-water marks plus conservative safety margin captured from runtime_health logs. In the same wave-3 gate, implement explicit fragmentation reduction in hot paths by replacing repeated short-lived heap allocations with reusable/pre-allocated buffers in `mqtt_dataplane.c` and `whep_endpoint.c` where safe lifetime boundaries already exist. Keep baseline constants and allocation behavior unchanged when gate is off. Do not estimate by intuition; derive values from measured evidence and explicitly log chosen margins plus fragmentation-trend markers for traceability.</action>
  <verify>`idf.py build` succeeds in baseline and wave-3 configurations, and serial logs show (a) measured high-water + margin rationale for tuned stacks and (b) largest-free-block/fragmentation-risk trend evidence that is stable or improved in wave-3 runs.</verify>
  <done>Wave-3 delivers reversible stack reductions and explicit fragmentation-risk reduction, both default-off and backed by largest-free-block trend evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Extend manual validation matrix for per-bundle and combined-bundle runs</name>
  <files>docs/manual-test-plan.md</files>
  <action>Add a Phase 2.1 matrix covering four explicit builds: wave-1 only, wave-2 only, wave-3 only, and combined (1+2+3). For each build, include mandatory scenarios (boot + idle + touchscreen), required runtime contract checks, and capture points for RAM attribution deltas and queue-full/stack-risk transition evidence.</action>
  <verify>Manual test plan includes all four required bundle combinations with mandatory scenario and evidence steps.</verify>
  <done>Executor can run both per-change and combined validation using one deterministic matrix aligned to locked decisions.</done>
</task>

</tasks>

<verification>
Run `idf.py build` for baseline, wave-2 only, wave-3 only, and combined bundle configs; confirm all new gates remain default-off in committed defaults.
</verification>

<success_criteria>
Second-priority MQTT queue-depth reduction and measured stack right-sizing are implemented as reversible default-off bundles with clear per-bundle/combined validation instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-03-SUMMARY.md`
</output>
