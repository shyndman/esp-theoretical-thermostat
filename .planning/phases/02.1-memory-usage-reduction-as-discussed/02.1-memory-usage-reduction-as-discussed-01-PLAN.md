---
phase: 02.1-memory-usage-reduction-as-discussed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/connectivity/runtime_health.h
  - main/connectivity/runtime_health.c
  - main/streaming/webrtc_stream.c
  - main/streaming/whep_endpoint.c
  - main/Kconfig.projbuild
  - sdkconfig.defaults
  - docs/manual-test-plan.md
autonomous: true

must_haves:
  truths:
    - "Operator can attribute internal-RAM pressure to explicit buckets (WebRTC pools, MQTT queue footprint, stack budgets, display/LVGL budget, dynamic allocation churn) from local logs."
    - "WebRTC reduction bundle is gated behind a new default-off config flag and can be enabled without changing display quality behavior."
    - "Baseline build behavior is unchanged when all new Phase 2.1 flags remain disabled."
  artifacts:
    - path: "main/connectivity/runtime_health.c"
      provides: "Periodic RAM attribution output and churn counters merged with existing runtime-health cadence"
      contains: "runtime_health_periodic_tick"
    - path: "main/streaming/webrtc_stream.c"
      provides: "Config-gated WebRTC pool/cache tuning profile for wave-1 RAM reduction"
      contains: "esp_peer_default_cfg_t"
    - path: "main/Kconfig.projbuild"
      provides: "Phase 2.1 tuning gates with explicit default-off policy"
      contains: "CONFIG_THEO_RAM_WAVE1_WEBRTC_TUNING"
  key_links:
    - from: "main/connectivity/runtime_health.c"
      to: "main/streaming/webrtc_stream.c"
      via: "Runtime-health attribution reads effective WebRTC pool/cache sizing used by wave-1 gate"
      pattern: "webrtc"
    - from: "main/Kconfig.projbuild"
      to: "main/streaming/webrtc_stream.c"
      via: "WebRTC tuning branch selected by new default-off config gate"
      pattern: "CONFIG_THEO_RAM_WAVE1_WEBRTC_TUNING"
    - from: "main/streaming/whep_endpoint.c"
      to: "main/connectivity/runtime_health.c"
      via: "WHEP allocation counters feed dynamic allocation churn bucket evidence"
      pattern: "malloc|calloc|free"
---

<objective>
Implement Phase 2.1 wave-1 by adding actionable RAM attribution and a default-off WebRTC pool reduction bundle.

Purpose: Start with the largest expected RAM win (WebRTC pools) while producing concrete bucket-level evidence that guides later waves.
Output: Runtime RAM attribution logs, dynamic allocation churn counters, and a config-gated WebRTC tuning bundle that is disabled by default.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-CONTEXT.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-RESEARCH.md
@.planning/phases/02-stack-and-heap-observability/02-stack-and-heap-observability-02-SUMMARY.md
@main/connectivity/runtime_health.c
@main/streaming/webrtc_stream.c
@main/streaming/whep_endpoint.c
@main/Kconfig.projbuild
@sdkconfig.defaults
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concrete RAM attribution buckets on existing runtime-health cadence</name>
  <files>main/connectivity/runtime_health.h, main/connectivity/runtime_health.c, main/streaming/webrtc_stream.c, main/streaming/whep_endpoint.c</files>
  <action>Extend runtime-health output with a structured RAM attribution block that reports explicit bytes/ratios for: display/LVGL budget (from compiled config), WebRTC pool/cache budget, MQTT queue footprint estimate, configured stack budget sum, and dynamic allocation churn deltas from known hot paths (`webrtc_stream`, `whep_endpoint`). Reuse the existing `heap_log_timer_cb`-driven runtime-health cadence and pre-allocated state only; do not add a second timer or per-tick heap allocation. Keep output local serial logs only.</action>
  <verify>`idf.py build` succeeds and serial output includes a periodic attribution line with all required bucket keys.</verify>
  <done>One periodic local log line exposes all required RAM attribution buckets and churn deltas in parseable key=value format.</done>
</task>

<task type="auto">
  <name>Task 2: Add default-off WebRTC wave-1 tuning gate and tuned pool profile</name>
  <files>main/Kconfig.projbuild, sdkconfig.defaults, main/streaming/webrtc_stream.c</files>
  <action>Add `CONFIG_THEO_RAM_WAVE1_WEBRTC_TUNING` in Kconfig with `default n`, then branch WebRTC peer pool/cache settings in `webrtc_stream.c` between current baseline and a reduced profile aimed at internal-RAM relief. Keep display/touch rendering paths untouched (no LVGL quality tradeoffs) and preserve required runtime capabilities. Ensure all new phase tuning flags remain default-off throughout this phase per locked decision.</action>
  <verify>`idf.py build` succeeds with gate disabled (baseline) and with `CONFIG_THEO_RAM_WAVE1_WEBRTC_TUNING=y` (tuned bundle).</verify>
  <done>Wave-1 WebRTC reductions are fully config-gated, default-off, and selectable without changing non-WebRTC baseline behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase-specific wave-1 validation instructions and failure-note format</name>
  <files>docs/manual-test-plan.md</files>
  <action>Add a dedicated "Phase 2.1 Wave-1 (WebRTC)" section with required scenario steps (boot + idle + touchscreen + one WebRTC session start/stop), required contract checks (network/MQTT/display/touchscreen), and a short failure-note template for triage when a bundle is rejected. Keep acceptance source as human sign-off only and do not prescribe automatic pass/fail beyond evidence capture.</action>
  <verify>Manual test plan contains explicit wave-1 steps and required-contract checks aligned with CONTEXT.md locked decisions.</verify>
  <done>Executor has deterministic wave-1 validation steps and triage-note format before human sign-off checkpoint.</done>
</task>

</tasks>

<verification>
Run `idf.py build` for baseline and wave-1-enabled configs, then confirm runtime-health logs emit RAM attribution buckets and churn evidence without adding timers.
</verification>

<success_criteria>
Wave-1 WebRTC tuning is implemented behind a default-off gate, and the firmware now emits concrete RAM attribution buckets that can drive wave-2 and wave-3 decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-01-SUMMARY.md`
</output>
