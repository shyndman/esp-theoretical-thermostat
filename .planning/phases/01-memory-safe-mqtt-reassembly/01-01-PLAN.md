---
phase: 01-memory-safe-mqtt-reassembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/connectivity/mqtt_dataplane.c
  - main/connectivity/mqtt_dataplane.h
  - main/app_main.c
  - docs/manual-test-plan.md
autonomous: true
must_haves:
  truths:
    - "Device reassembles fragmented MQTT payloads with a hard 1024-byte cap and no hot-path fragment/reassembly heap allocation."
    - "Device accepts only one active fragmented flow and uses freshness-first preemption: a valid offset=0 newcomer replaces active flow with no timeout recovery path."
    - "Device drops invalid fragments with exactly one reason from {oversize, out_of_order, nonzero_first, overlap, queue_full} and tracks queue_full in digest output."
    - "Device emits an ESP_LOGI digest every 60 seconds wall-clock as a single structured line, always printed, including cumulative totals and per-interval deltas, and explicitly includes preempted-flow counting."
  artifacts:
    - path: "main/connectivity/mqtt_dataplane.c"
      provides: "Single-slot PSRAM-backed reassembly state machine with hardcoded compile-time constants and mandatory drop taxonomy"
      contains: "oversize"
    - path: "main/connectivity/mqtt_dataplane.h"
      provides: "Dataplane periodic digest hook/API used by heap timer callback"
      contains: "mqtt_dataplane"
    - path: "main/app_main.c"
      provides: "Heap timer callback piggyback to trigger dataplane digest cadence"
      contains: "heap_log_timer_cb"
  key_links:
    - from: "main/connectivity/mqtt_dataplane_event_handler"
      to: "main/connectivity/fragment state machine"
      via: "xQueueSend path classifies queue pressure as queue_full"
      pattern: "xQueueSend\\(s_msg_queue"
    - from: "main/app_main.c"
      to: "main/connectivity/mqtt_dataplane.c"
      via: "heap_log_timer_cb invokes dataplane digest tick (no separate timer)"
      pattern: "mqtt_dataplane"
    - from: "main/connectivity/drop classification"
      to: "main/connectivity/periodic digest log"
      via: "single-reason counters roll into cumulative+delta structured line"
      pattern: "ESP_LOGI\\(TAG"
    - from: "main/connectivity/freshness-first preemption path"
      to: "main/connectivity/periodic digest log"
      via: "preempted-flow counter increments and is emitted as cumulative+delta digest fields"
      pattern: "preempt"
---

<objective>
Implement Phase 1 bounded MQTT fragment reassembly with exact context-locked behavior for memory bounds, drop taxonomy, and digest cadence.

Purpose: Satisfy MEM-01/02/03 while honoring locked decisions exactly (1024 cap, PSRAM placement, topic bound 120, freshness-first preemption, no timeout, one-reason drop accounting, and heap-timer-piggyback digest).
Output: Updated dataplane FSM + counters/digest + heap timer integration + manual verification coverage for Phase 1 only.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-memory-safe-mqtt-reassembly/01-CONTEXT.md
@.planning/phases/01-memory-safe-mqtt-reassembly/01-RESEARCH.md
@main/connectivity/mqtt_dataplane.c
@main/connectivity/mqtt_dataplane.h
@main/app_main.c
@docs/manual-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement locked single-slot reassembly policy and hardcoded bounds</name>
  <files>main/connectivity/mqtt_dataplane.c</files>
  <action>Replace current multi-slot/dynamic fragment handling with one active reassembly context whose payload buffer is placed in PSRAM. Define hardcoded compile-time constants directly in dataplane code for payload cap (1024 bytes) and cached topic bound (120 chars); do not add or modify Kconfig for these values. Enforce freshness-first preemption exactly: when a valid newcomer starts at offset 0 while an older flow is active, preempt the active flow and continue with newcomer; do not add timeout-based recovery in this phase. Implement deterministic one-reason-per-drop classification with fixed precedence chosen in code for only these reasons: oversize, out_of_order, nonzero_first, overlap, queue_full. Do not add missing_topic_first handling/counter in this phase.</action>
  <verify>Run `idf.py build`; inspect `main/connectivity/mqtt_dataplane.c` to confirm constants are local compile-time defines (not `CONFIG_THEO_*`), payload buffer uses PSRAM placement attribute, and drop-reason enums/strings are limited to oversize/out_of_order/nonzero_first/overlap/queue_full.</verify>
  <done>Dataplane enforces the exact Phase 1 locked reassembly behavior with hard bounds, PSRAM payload buffer, freshness-first preemption, no timeout logic, and mandatory taxonomy-only drop accounting.</done>
</task>

<task type="auto">
  <name>Task 2: Add periodic digest accounting contract on dataplane side</name>
  <files>main/connectivity/mqtt_dataplane.c, main/connectivity/mqtt_dataplane.h</files>
  <action>Introduce dataplane digest state that tracks cumulative counters and previous-snapshot values to compute deltas per emission window. Emit digest as one ESP_LOGI structured key=value line containing both totals and deltas for each mandatory drop reason (including queue_full) and an explicit preempted-flow counter field (totals + deltas) so freshness-first preemptions are always visible in digest output per locked decision. Digest must always print once per 60-second wall-clock interval even when all deltas are zero. Expose a small dataplane API callable from app_main heap timer callback (for example `mqtt_dataplane_periodic_tick(now_us)`) so cadence is externally triggered without creating a new timer in dataplane.</action>
  <verify>Run `idf.py build`; inspect `main/connectivity/mqtt_dataplane.c` log formatting to confirm one structured ESP_LOGI line with cumulative and delta fields where queue_full and the preempted-flow counter each appear in both sections.</verify>
  <done>Dataplane contains the locked digest contract: 60-second wall-clock gating, always-print heartbeat behavior, and cumulative+delta single-line structured output including queue_full and preempted-flow counter fields.</done>
</task>

<task type="auto">
  <name>Task 3: Piggyback digest emission on heap timer and document verification matrix</name>
  <files>main/app_main.c, docs/manual-test-plan.md</files>
  <action>Update existing heap timer callback in `app_main.c` to invoke the new dataplane periodic tick so digest cadence piggybacks the heap monitor timer; do not create a second periodic timer anywhere for this phase. Keep digest cadence wall-clock based at 60 seconds in dataplane logic. Extend `docs/manual-test-plan.md` with Phase 1 verification cases for: valid in-order reassembly, oversize, out_of_order, nonzero_first, overlap freshness-first preemption, and queue_full. For each case specify expected one-reason accounting and expected digest evidence (cumulative + delta, single-line format, heartbeat line present even at zero delta).
  </action>
  <verify>Run `idf.py build`; verify `main/app_main.c` heap timer callback calls dataplane tick API; review `docs/manual-test-plan.md` to ensure all five mandatory drop reasons plus heartbeat digest behavior are explicitly covered.</verify>
  <done>Digest scheduling is piggybacked on existing heap timer infrastructure, and manual validation steps prove locked Phase 1 behavior without introducing Phase 2/3 scope.</done>
</task>

</tasks>

<verification>
1) `idf.py build` passes with dataplane and app_main updates.
2) `main/connectivity/mqtt_dataplane.c` enforces hardcoded constants (1024 payload, 120 topic), PSRAM payload storage, freshness-first preemption, no timeout recovery, and taxonomy-only drop reasons.
3) Digest path logs one ESP_LOGI structured line every 60s wall-clock (always printed) with cumulative+delta counters including queue_full and preempted-flow counts.
4) `docs/manual-test-plan.md` covers MEM-01/02/03 scenarios only and verifies one-reason-per-drop behavior for all mandatory reasons.
</verification>

<success_criteria>
1) MEM-01: Fragment reassembly operates with bounded pre-allocated capacity using a 1024-byte cap and no hot-path allocation churn.
2) MEM-02: Invalid fragments are dropped with deterministic, one-reason accounting from the mandatory taxonomy and visible in minute digest lines.
3) MEM-03: Exactly one fragmented flow is active, with freshness-first newcomer preemption and no timeout-based fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-safe-mqtt-reassembly/01-memory-safe-mqtt-reassembly-01-SUMMARY.md`
</output>
