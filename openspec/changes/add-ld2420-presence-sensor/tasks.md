## Implementation
- [ ] Add Kconfig entries for LD2420: `CONFIG_THEO_LD2420_ENABLE`, `CONFIG_THEO_LD2420_UART_NUM` (2), `CONFIG_THEO_LD2420_UART_RX_GPIO` (38), `CONFIG_THEO_LD2420_UART_TX_GPIO` (37), `CONFIG_THEO_LD2420_CLOSE_DISTANCE_CM` (100), `CONFIG_THEO_LD2420_CLOSE_HOLD_MS` (1000), `CONFIG_THEO_LD2420_OFFLINE_TIMEOUT_MS` (10000).
- [ ] Create `main/sensors/ld2420.h` with public API: `ld2420_start()`, `ld2420_get_presence()`, `ld2420_get_distance_cm()`, `ld2420_register_close_approach_cb()`, and relevant types.
- [ ] Implement `main/sensors/ld2420.c`: UART initialization, config mode entry/exit, Energy mode frame parsing, FreeRTOS task for continuous reading.
- [ ] Implement close-approach debounce logic: track approach start time, fire callbacks after hold time elapses, reset on presence loss.
- [ ] Add MQTT publishing for presence (binary_sensor) and distance (sensor) following Theo namespace pattern; publish every 5 seconds when data is fresh.
- [ ] Emit Home Assistant discovery payloads for `radar_presence` (binary_sensor, device_class: occupancy) and `radar_distance` (sensor, device_class: distance, unit: cm).
- [ ] Emit availability messages (`online`/`offline`) on sensor init success/failure and during runtime if communication is lost.
- [ ] Integrate into `app_main.c` boot sequence: call `ld2420_start()` after MQTT manager, log warning if sensor absent, continue boot regardless.
- [ ] Update `docs/manual-test-plan.md` with LD2420 validation steps: presence detection, distance accuracy, MQTT topic verification, close-approach callback timing.
- [ ] Validate with `openspec validate add-ld2420-presence-sensor --strict` and run `idf.py build` to confirm compilation.
