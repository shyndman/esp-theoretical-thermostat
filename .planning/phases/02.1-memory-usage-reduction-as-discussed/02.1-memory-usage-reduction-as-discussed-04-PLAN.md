---
phase: 02.1-memory-usage-reduction-as-discussed
plan: 04
type: execute
wave: 4
depends_on:
  - 02.1-memory-usage-reduction-as-discussed-03
files_modified:
  - .planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-04-EVIDENCE.md
  - .planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-04-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Each reduction bundle and the combined bundle has human sign-off results from boot + idle + touchscreen validation."
    - "Required runtime contract is preserved after tuning (network, MQTT, display, touchscreen)."
    - "Phase 2.1 exits with explicit accepted/rejected bundle set and triage notes for any rejected bundle."
  artifacts:
    - path: "docs/manual-test-plan.md"
      provides: "Per-bundle and combined validation steps used for final human acceptance"
      contains: "Phase 2.1"
    - path: "main/Kconfig.projbuild"
      provides: "Wave-1/2/3 tuning gates participating in combined validation"
      contains: "CONFIG_THEO_RAM_WAVE"
  key_links:
    - from: "bundle configs (wave-1, wave-2, wave-3, combined)"
      to: "human acceptance"
      via: "Mandatory boot + idle + touchscreen matrix execution"
      pattern: "approved|rejected"
    - from: "required runtime contract checks"
      to: "bundle decision"
      via: "Retry-once policy and reject-on-repeat for boot capability failures"
      pattern: "network|mqtt|display|touchscreen"
---

<objective>
Run final Phase 2.1 acceptance gates for per-bundle and combined-bundle validation.

Purpose: Enforce the locked human-only acceptance signal before any default enablement decisions and capture explicit go/no-go outcomes.
Output: Final human-approved/rejected bundle outcomes with required-contract evidence and triage notes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-CONTEXT.md
@.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-03-SUMMARY.md
@docs/manual-test-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute per-bundle and combined validation runs and collect evidence</name>
  <files>.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-04-EVIDENCE.md</files>
  <action>Run the Phase 2.1 validation matrix builds/runs for wave-1 only, wave-2 only, wave-3 only, and combined (1+2+3). For each run, collect serial evidence for required runtime contract and RAM attribution deltas, then append to `04-EVIDENCE.md` using one section per bundle run. Include a compact trend table for largest free block and fragmentation-risk indicators across runs so final acceptance can confirm fragmentation-risk direction does not regress. Apply retry-once policy for required-capability boot failures; on repeat failure mark that bundle rejected and capture failure notes only for triage.</action>
  <verify>All four bundle runs produce evidence logs in `04-EVIDENCE.md` with explicit pass/fail observations plus largest-free-block/fragmentation-risk trend entries.</verify>
  <done>Final human verification can evaluate complete per-bundle and combined evidence, including fragmentation-risk trend data, without additional implementation work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off for final Phase 2.1 acceptance</name>
  <files></files>
  <action>Run final human verification across per-bundle and combined runs, then record the accepted/rejected bundle set for Phase 2.1 completion.</action>
  <verify>Human response explicitly confirms accepted bundles (or rejected bundles with notes) after reviewing all required checks.</verify>
  <done>Final Phase 2.1 acceptance outcome is explicit and ready to drive the next phase decision.</done>
  <what-built>All config-gated RAM reduction bundles (wave-1 WebRTC, wave-2 MQTT queue depth, wave-3 stack right-sizing) plus combined run evidence.</what-built>
  <how-to-verify>
    1) Review evidence for each bundle and combined run against mandatory scenarios (boot + idle + touchscreen).
    2) Confirm required runtime contract remains intact in accepted runs: network up, MQTT connected, display intact, touchscreen responsive.
    3) Confirm no display-quality regression was introduced by accepted bundles.
    4) For mixed outcomes, choose case-by-case accepted/rejected set and keep rejection notes for triage.
  </how-to-verify>
  <resume-signal>Type "approved" with accepted bundle set, or provide rejected bundle notes for follow-up.</resume-signal>
</task>

</tasks>

<verification>
Phase 2.1 completes only after explicit human acceptance outcome is recorded for per-bundle and combined validation.
</verification>

<success_criteria>
Phase 2.1 has a human-validated accepted bundle set, rejected bundles are triaged, and required runtime contract remains intact for accepted configurations.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-memory-usage-reduction-as-discussed/02.1-memory-usage-reduction-as-discussed-04-SUMMARY.md`
</output>
