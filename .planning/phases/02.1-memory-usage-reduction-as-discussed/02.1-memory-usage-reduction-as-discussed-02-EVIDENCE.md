# Phase 2.1 Plan 02 Wave-1 Evidence

## Execution context

- Timestamp (UTC): 2026-02-10T17:01:54Z
- Wave-1 flag intent: `CONFIG_THEO_RAM_WAVE1_WEBRTC_TUNING=y`
- Build directory: `build-wave1`
- SDK config used for wave-1 build: `tmp/sdkconfig.wave1`

## Build and flash-prep evidence

### Wave-1 build (pass)

Command:

```bash
idf.py -B build-wave1 -DSDKCONFIG="/home/shyndman/dev/projects/esp-theoretical-thermostat/tmp/sdkconfig.wave1" build
```

Evidence snippet:

```text
[8/9] Generating binary image from built executable
Successfully created esp32p4 image.
Generated /home/shyndman/dev/projects/esp-theoretical-thermostat/build-wave1/esp_theoretical_thermostat.bin
esp_theoretical_thermostat.bin binary size 0x355c30 bytes.
Project build complete.
```

### Wave-1 flash attempts (retry-once policy applied)

Commands (attempt 1 + retry):

```bash
idf.py -B build-wave1 flash
idf.py -B build-wave1 flash
```

Evidence snippet:

```text
No serial ports found. Connect a device, or use '-p PORT' option to set a specific port.
Serial port /dev/ttyS9 ... failed to connect
...
Serial port /dev/ttyS0 ... failed to connect
```

Result:

- Flash could not proceed because no usable serial device was available in this execution environment.
- Retry-once policy was executed and produced the same failure.

## Required runtime-contract evidence status

Because flash failed twice, runtime boot logs and live behavior checks could not be captured in this run.

| Required capability | Evidence status | Notes |
| --- | --- | --- |
| Network ready | Not captured | Device did not boot from this session (flash blocked). |
| MQTT connected | Not captured | Device did not boot from this session (flash blocked). |
| Display rendered | Not captured | Device did not boot from this session (flash blocked). |
| Touch events accepted | Not captured | Device did not boot from this session (flash blocked). |

## Baseline vs wave-1 runtime-health comparison

No new serial `runtime_health_obs`/`runtime_health_ram_attr` lines were available from this run because the device could not be flashed/booted.

| Metric | Baseline | Wave-1 | Trend note |
| --- | --- | --- | --- |
| `heap_internal_largest_b` | Not captured in this run | Not captured in this run | Blocked by flash/connectivity failure before boot. |
| `heap_internal_risk` | Not captured in this run | Not captured in this run | Blocked by flash/connectivity failure before boot. |

## Deviation notes captured during Task 1

1. [Rule 1 - Bug] Wave-1 enabled build initially failed due an unused baseline config symbol in `main/streaming/webrtc_stream.c`; fixed by compiling only the active profile constant and using compile-time wave flag in log output.

## Task 1 disposition

- Wave-1 evidence package is prepared with build proof and retry-once flash failure proof.
- Boot/idle/touch/WebRTC runtime evidence remains pending hardware connectivity.
- This should be treated as a rejection signal for this run until hardware access is restored and the runtime contract can be observed.
